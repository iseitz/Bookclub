<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collpase" data-target="#bs-example-navbar-collpase-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>


          <div id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li>
                <%= link_to "View All Reviews", books_path, class: "navbar-brand" %>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                          View By Age Group
                </a>
                <ul class="dropdown-menu">
                  <% @age_groups = AgeGroup.all %>
                  <% @age_groups.each do |age_group| %>
                      <% @range = age_group.group %>
                      <li><%= link_to "#{@range}", age_group_books_path(age_group), class: "navbar-brand" %></li>
                  <% end %>
                </ul>
                <div class="clear"></div>
              </li>
      </ul>
    

     <!-- Collect the nav links, forms, and other content for toggling -->

       <ul class="nav navbar-nav">
         <% if user_signed_in? %>
            <li><%= link_to "Create Review", new_book_path, class: "active" %></li>
            <li><%= link_to "#{current_user.username}'s' Account", user_path(current_user), class: "active" %></li>
            <li><%= link_to "Log out", destroy_user_session_path, :method => 'delete', class: "active" %></li>
         <% else %>
            <li><%= link_to "Sign Up", new_user_registration_path, class: "active" %></li>
            <li><%= link_to "Log in", new_user_session_path, class: "active" %></li>

         <% end%>
       </ul>
         <%= form_tag search_books_path, :method => 'get', class: "navbar-form navbar-right", role: "search" do %>
            <p>
              <%= text_field_tag :search, params[:search], class: "form-control" %>
              <%= submit_tag "Search", name: nil, class: "btn btn-default" %>
            </p>
         <% end %>
    </div> <!-- /.navbar-collpase -->
  </div> <!-- /.container-fluid -->
</nav>
